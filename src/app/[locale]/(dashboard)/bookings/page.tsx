"use client";

import { PlaceholderPage } from "@/components/common/PlaceholderPage";

export default function NewBookingPage() {
  return <PlaceholderPage title="New Booking" description="The booking creation flow is currently being redesigned to match the new port operations interface." />;
}
