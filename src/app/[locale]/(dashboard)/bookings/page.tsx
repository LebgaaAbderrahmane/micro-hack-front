"use client";

import { PlaceholderPage } from "@/components/common/PlaceholderPage";
import useDebounce from "@/hooks/useDebounce";

export default function BookingsPage() {
  return <PlaceholderPage title="Logging" description="The logging module is currently being redesigned to match the new port operations interface." />;
}
