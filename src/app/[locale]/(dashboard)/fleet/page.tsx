"use client";

import { PlaceholderPage } from "@/components/common/PlaceholderPage";

export default function FleetPage() {
  return <PlaceholderPage title="Manage" description="Fleet and resource management is currently being redesigned to match the new port operations interface." />;
}
