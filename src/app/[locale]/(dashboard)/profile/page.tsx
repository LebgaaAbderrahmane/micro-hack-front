"use client";

import { PlaceholderPage } from "@/components/common/PlaceholderPage";

export default function ProfilePage() {
  return <PlaceholderPage title="Profile" description="User profile is currently being redesigned to match the new port operations interface." />;
}
