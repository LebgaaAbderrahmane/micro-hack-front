"use client";

import { PlaceholderPage } from "@/components/common/PlaceholderPage";

export default function UsersPage() {
  return <PlaceholderPage title="User Management" description="User management is currently being redesigned to match the new port operations interface." />;
}
