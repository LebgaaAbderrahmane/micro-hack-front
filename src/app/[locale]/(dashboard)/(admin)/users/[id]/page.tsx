"use client";

import { PlaceholderPage } from "@/components/common/PlaceholderPage";

export default function UserDetailPage() {
  return <PlaceholderPage title="User Details" description="User detail view is currently being redesigned to match the new port operations interface." />;
}
