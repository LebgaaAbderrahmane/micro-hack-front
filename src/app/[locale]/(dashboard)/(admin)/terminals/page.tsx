"use client";

import { PlaceholderPage } from "@/components/common/PlaceholderPage";

export default function TerminalsPage() {
  return <PlaceholderPage title="Terminal Management" description="Terminal management is currently being redesigned to match the new port operations interface." />;
}
